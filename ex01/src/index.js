function main(numOne, numTwo) { 

    var quotient;
    if(numTwo === 0){
        quotient = 'Davide by 0! Cannot divide by yero!';
    } else { 
        // Only change code below this line

        var numOne;
        var numTwo;
        var quotient; 

        numOne = 15;
        numTwo = 3;
        quotient = numOne / numTwo;


        // Only change code above this line

     } 

return quotient;
}

console.log(main(15, 3)); // change this line
module.exports = main